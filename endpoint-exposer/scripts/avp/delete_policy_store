#!/bin/bash

set -euo pipefail

# The AVP policy store is infrastructure-managed (provisioned via Terraform).
# Services must never delete it — it is shared across all services in this cluster.
echo "AVP policy store is infrastructure-managed. Skipping deletion."
